# IRH Cloud Run Requirements
# Minimal dependencies for Google Cloud Run deployment
# THEORETICAL FOUNDATION: IRH v21.1 Manuscript

# Core API framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# CORS and request handling
python-multipart>=0.0.6

# Scientific computing (IRH core modules)
numpy>=1.24.0
scipy>=1.10.0
sympy>=1.12

# Data handling
pyyaml>=6.0

# Testing (optional, for health checks)
httpx>=0.25.0

# Note: Excluded from Cloud Run deployment:
# - celery, redis (not needed for stateless Cloud Run)
# - websockets (Cloud Run doesn't support WebSockets in the standard way)
# - black, isort, flake8, mypy (development tools)
# - pytest (testing tools)
# - jupyter (notebook tools)
# - h5py (large data files should be external)
