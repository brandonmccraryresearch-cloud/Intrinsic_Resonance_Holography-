# IRH v21.1 ConfigMap
# Configuration values for the application

apiVersion: v1
kind: ConfigMap
metadata:
  name: irh-config
  namespace: irh
data:
  # API Configuration
  LOG_LEVEL: "INFO"
  API_HOST: "0.0.0.0"
  API_PORT: "8000"
  
  # IRH Configuration
  IRH_VERSION: "21.1.0"
  
  # Fixed-point values (Eq. 1.14)
  LAMBDA_STAR: "52.637890139440744"
  GAMMA_STAR: "105.27578027888149"
  MU_STAR: "157.91367041832223"
  
  # Universal exponent (Eq. 1.16)
  C_H: "0.045935703598"
  
  # Fine-structure constant (Eq. 3.4-3.5)
  ALPHA_INVERSE: "137.035999084"
  
  # Dark energy EoS (§2.3.3)
  W_0: "-0.91234567"
# IRH v21.1 Secret (template)
# SECURITY: This is a placeholder - replace with actual secrets in production
# DO NOT use this value in production environments
# Use Kubernetes secrets management or a secrets manager (e.g., Vault, AWS Secrets Manager)
apiVersion: v1
kind: Secret
metadata:
  name: irh-secrets
  namespace: irh
  annotations:
    description: "Template secret - replace values before deployment"
type: Opaque
stringData:
  # IMPORTANT: Replace this placeholder before production deployment
  # Generate with: openssl rand -base64 32
  API_SECRET_KEY: "REPLACE_WITH_SECURE_KEY_BEFORE_DEPLOYMENT"
